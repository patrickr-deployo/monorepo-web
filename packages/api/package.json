{
  "name": "@package/api",
  "version": "0.0.0",
  "private": true,
  "exports": "./index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build -w --preserveWatchOutput --incremental",
    "openapi": "sh ./generate.sh",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives",
    "format": "eslint . --ext ts,tsx --fix",
    "clean": "rimraf dist",
    "purge": "rimraf dist && rimraf node_modules"
  },
  "dependencies": {
    "axios": "^1.7.5"
  },
  "devDependencies": {
    "@package/eslint-config": "workspace:*",
    "@package/prettier-config": "workspace:*",
    "@package/typescript-config": "workspace:*",
    "@package/api-config": "workspace:*"
  }
}
